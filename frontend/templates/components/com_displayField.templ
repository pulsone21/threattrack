package components

templ DisplayItem(lable, value string) {
	<div class="flex flex-col">
		<span class="text-lg font-black">{ lable }</span>
		<span class="ml-4 font-light text-base text-font-100">{ value }</span>
	</div>
}
